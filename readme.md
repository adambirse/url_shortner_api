# Url Shortner API

An API that provides a url shortning service.  i.e. tinyurl.

`dotnet run`

`dotnet test`

## Create DLL

`dotnet publish`

## Run release version

`dotnet run path/to/release.dll`
